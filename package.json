{
  "name": "toxiscan",
  "version": "2.0.0",
  "description": "YouTube Comment Toxicity Analyzer — multilingual analysis, user dashboards, and an evaluation console",
  "scripts": {
    "install:all": "npm install --prefix frontend-next",
    "dev": "npm run dev --prefix frontend-next",
    "build": "npm run build --prefix frontend-next",
    "start": "npm run start --prefix frontend-next",
    "typecheck": "npm run typecheck --prefix frontend-next",
    "lint": "npm run lint --prefix frontend-next",
    "db:migrate": "npm run db:migrate --prefix frontend-next",
    "db:seed": "npm run db:seed --prefix frontend-next",
    "db:setup": "npm run db:setup --prefix frontend-next",
    "db:studio": "npm run db:studio --prefix frontend-next",
    "check": "npm run check --prefix frontend-next"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
